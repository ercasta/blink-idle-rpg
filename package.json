{
  "name": "blink-idle-rpg-root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "_comment_setup": "── Install npm dependencies ──────────────────────────────",
    "setup": "npm --prefix packages/blink-compiler-ts install && npm --prefix packages/blink-engine-wasm install && npm --prefix packages/blink-engine-wasm-js install",
    "_comment_build": "── Build TypeScript compiler ─────────────────────────────",
    "build:compiler:ts": "npm --prefix packages/blink-compiler-ts run build",
    "build:compiler:bundle": "npm --prefix packages/blink-compiler-ts run build:bundle",
    "build:compiler": "npm run build:compiler:ts && npm run build:compiler:bundle",
    "_comment_brl": "── Compile BRL game rules to IR ──────────────────────────",
    "compile-brl": "npm run build:compiler:ts && node tools/compile-brl-to-ir.js",
    "_comment_gamefiles": "── Copy BRL/BCL to web app public dir ────────────────────",
    "copy-game-files": "node scripts/copy-game-files.js",
    "_comment_app": "── React web app (full UI) ────────────────────────────────",
    "dev:app": "npm run copy-game-files && npm --prefix game/app run dev",
    "build:app": "npm run copy-game-files && npm --prefix game/app run build",
    "_comment_test": "── Test harness (BRL → Rust → native, requires Rust) ─────",
    "test:harness": "npm run build:compiler:ts && npm --prefix packages/blink-engine-wasm test",
    "test:compiler": "npm --prefix packages/blink-compiler-ts run test",
    "_comment_wasm": "── WASM engine (requires Rust + wasm-pack) ───────────────",
    "build:wasm": "npm run build:compiler:ts && npm --prefix packages/blink-engine-wasm-js install && npm --prefix packages/blink-engine-wasm-js run build",
    "build:wasm:dev": "npm run build:compiler:ts && npm --prefix packages/blink-engine-wasm-js install && npm --prefix packages/blink-engine-wasm-js run build:dev",
    "install:wasm": "node scripts/install-wasm.js",
    "_comment_batch": "── Batch simulation (game designer balancing tool) ───────",
    "simulate": "npm run build:wasm && node tools/simulate.js",
    "_comment_harness": "── Massive test harness (design validation tool) ────────",
    "test:massive": "npm run build:compiler:ts && node tools/massive-test-harness.js",
    "_comment_full": "── Full production build ──────────────────────────────────",
    "build": "npm run build:compiler && npm run build:wasm && npm run install:wasm && npm run build:app"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
