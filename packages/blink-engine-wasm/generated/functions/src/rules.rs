// Generated by blink-compiler-ts - do not edit
#![allow(dead_code, unused_imports, unused_variables, unused_mut)]

use blink_runtime::{Engine, World, Timeline, Event, InternedString, Value, EntityId, NO_ENTITY};
use blink_runtime::builtins::*;
use crate::components::*;
use crate::functions::*;
use crate::string_ids;

pub fn rule_anonymous_0(event: &Event, engine: &mut Engine) {
    let gs_source = event.source;
    {
        let mut sched_event = blink_runtime::Event::new(string_ids::STR_COMPUTE_DAMAGE);
        engine.timeline.schedule_delay(0.0 as f64, sched_event);
    }
}

pub fn rule_anonymous_1(event: &Event, engine: &mut Engine) {
    let cd_source = event.source;
    let e_ids = engine.world.query_component::<Stats>();
    for e in e_ids {
        let mut dmg = calculate_damage(engine.world.get::<Stats>(e).base_damage, engine.world.get::<Stats>(e).bonus);
        engine.world.get_mut::<Stats>(e).effective_damage = dmg;
        engine.world.get_mut::<Result>(e).total = clamp_value(dmg, 0, 20);
        engine.world.get_mut::<Result>(e).computed = true;
    }
}

