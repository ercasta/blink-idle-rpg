[package]
name = "blink-runtime"
version = "0.1.0"
edition = "2024"
description = "Blink game runtime library for WASM engine - provides ECS, Timeline, and event dispatch"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
