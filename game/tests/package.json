{
  "name": "blink-examples-tests",
  "version": "0.1.0",
  "description": "Example tests for Blink Idle RPG game rules (run via WASM E2E harness)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "echo 'Tests migrating to WASM E2E harness — use npm run test:harness from root'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@blink/compiler-ts": "file:../../packages/blink-compiler-ts"
  }
}
