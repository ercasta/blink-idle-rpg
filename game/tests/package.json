{
  "name": "blink-examples-tests",
  "version": "0.1.0",
  "description": "Example tests for Blink Idle RPG game rules",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/*.test.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@blink/engine": "file:../../packages/blink-engine",
    "@blink/compiler-ts": "file:../../packages/blink-compiler-ts",
    "@blink/test": "file:../../packages/blink-test"
  }
}
